import * as React from 'react';
import Scene from '../Scene';
import { Text } from 'native-base';

export default class HoursListScene extends React.Component<{}, {}> {
  render() {
    return (
      <Scene>
        <Text>HoursListScene</Text>
      </Scene>
    );
  }
}
