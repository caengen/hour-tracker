import * as React from 'react';
import Scene from '../Scene';
import { Text } from 'native-base';

export default class HoursOverviewScene extends React.Component<{}, {}> {
  render() {
    return (
      <Scene>
        <Text>Drilling down to HoursOverview</Text>
      </Scene>
    );
  }
}
