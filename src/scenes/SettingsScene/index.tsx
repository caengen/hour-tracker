import * as React from 'react';
import Scene from '../Scene';
import { Text } from 'native-base';

export default class SettingsListScene extends React.Component<{}, {}> {
  render() {
    return (
      <Scene>
        <Text>SettingsListScene</Text>
      </Scene>
    );
  }
}
