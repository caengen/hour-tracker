import * as React from 'react';
import Scene from '../Scene';
import { Text } from 'native-base';

export default class PayPeriodsListScene extends React.Component<{}, {}> {
  render() {
    return (
      <Scene>
        <Text>PayPeriodsListScene</Text>
      </Scene>
    );
  }
}
